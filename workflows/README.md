# Workflows

Workflows describe how multiple skills, prompts, and checks fit together.

Use YAML for machine-readable recipes and Markdown for human-facing process notes.

Current workflows:

- `content-pipeline.yaml`: generic content drafting and formatting pipeline
- `travel-article-beautifier.yaml`: route raw travel material through intake, style routing, template selection, drafting, review, and delivery
